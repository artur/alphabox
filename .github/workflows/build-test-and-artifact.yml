name: Build, run test and upload binaries 

on: [push, pull_request]

jobs:

  linux-x86-clang:
    runs-on: "ubuntu-24.04"

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y-%m-%dT%H%M')" >> $GITHUB_OUTPUT

    - name: Install build dependencies
      run: sudo apt -y update && sudo apt -y install libpcap-dev netcat-openbsd libx11-dev libxext-dev libxrandr-dev libxcursor-dev libxi-dev libxtst-dev libxfixes-dev libxss-dev libxkbcommon-dev libwayland-dev libegl-dev

    - name: Create build environment
      run: cmake -E make_directory ${{runner.workspace}}/build

    - name: Configure CMake
      shell: bash
      working-directory: ${{runner.workspace}}/build
      run: cmake $GITHUB_WORKSPACE -DCMAKE_C_COMPILER="clang" -DCMAKE_CXX_COMPILER="clang++" -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_FLAGS="-Wall"

    - name: Build
      working-directory: ${{runner.workspace}}/build
      shell: bash
      run: cmake --build . --config Release
      env:
        MAKEFLAGS: "-j2"

    - name: Run test scripts
      working-directory: ${{runner.workspace}}/alphabox
      shell: bash
      run: ${{runner.workspace}}/alphabox/test/run

    - name: Upload Alphabox binary
      uses: actions/upload-artifact@v4
      with:
        name: Alphabox-linux-x86-clang-${{ env.BUILD_DATE }}
        path: ${{runner.workspace}}/build/alphabox 
      env:
        BUILD_DATE: ${{ steps.date.outputs.date }}

  linux-x86-gcc:
    runs-on: "ubuntu-24.04"

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y-%m-%dT%H%M')" >> $GITHUB_OUTPUT
      
    - name: Install gcovr
      run: pip install gcovr

    - name: Install build dependencies
      run: sudo apt -y update && sudo apt -y install libpcap-dev netcat-openbsd libx11-dev libxext-dev libxrandr-dev libxcursor-dev libxi-dev libxtst-dev libxfixes-dev libxss-dev libxkbcommon-dev libwayland-dev libegl-dev

    - name: Create build environment
      run: cmake -E make_directory ${{runner.workspace}}/build

    - name: Configure CMake
      shell: bash
      working-directory: ${{runner.workspace}}/build
      run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_FLAGS="-Wall" -DCMAKE_EXPORT_COMPILE_COMMANDS=1

    - name: Build
      working-directory: ${{runner.workspace}}/build
      shell: bash
      run: cmake --build . --config Release
      env:
        MAKEFLAGS: "-j2"

    - name: Run test scripts
      working-directory: ${{runner.workspace}}/alphabox
      shell: bash
      run: ${{runner.workspace}}/alphabox/test/run

    - name: Upload Alphabox binary
      uses: actions/upload-artifact@v4
      with:
        name: Alphabox-linux-x86-gcc-${{ env.BUILD_DATE }}
        path: ${{runner.workspace}}/build/alphabox
      env:
        BUILD_DATE: ${{ steps.date.outputs.date }} 

  linux-arm-gcc-crosscompile:
    runs-on: "ubuntu-24.04"
    continue-on-error: true

    steps:
    - uses: actions/checkout@v4

    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y-%m-%dT%H%M')" >> $GITHUB_OUTPUT

    - name: Install build dependencies
      run: sudo apt -y update && sudo apt -y install netcat-openbsd qemu-user qemu-user-binfmt gcc-arm-linux-gnueabihf g++-arm-linux-gnueabihf libc6-dev-armhf-cross

    - name: Create build environment
      run: cmake -E make_directory ${{runner.workspace}}/build

    - name: Configure CMake
      shell: bash
      working-directory: ${{runner.workspace}}/build
      run: cmake $GITHUB_WORKSPACE -DSTATIC_COMPILE=yes -DDISABLE_PCAP=yes -DDISABLE_SDL=yes -DDISABLE_X11=yes -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_FLAGS="-Wall" -DCMAKE_C_COMPILER=arm-linux-gnueabihf-gcc -DCMAKE_CXX_COMPILER=arm-linux-gnueabihf-g++ -DCMAKE_CXX_FLAGS="-O3 -mcpu=cortex-a7 -mfpu=neon -mfloat-abi=hard -mno-unaligned-access"

    - name: Build
      working-directory: ${{runner.workspace}}/build
      shell: bash
      run: cmake --build . --config Release
      env:
        MAKEFLAGS: "-j2"

    - name: Run binary
      working-directory: ${{runner.workspace}}/alphabox
      shell: bash
      run: ${{runner.workspace}}/build/alphabox

    - name: Binary info
      working-directory: ${{runner.workspace}}/alphabox
      shell: bash
      run: file ${{runner.workspace}}/build/alphabox

    - name: Upload Alphabox binary
      uses: actions/upload-artifact@v4
      with:
        name: Alphabox-linux-ARM-gcc-${{ env.BUILD_DATE }}
        path: ${{runner.workspace}}/build/alphabox
      env:
        BUILD_DATE: ${{ steps.date.outputs.date }}        

  windows-x86-msvc:
    runs-on: windows-2025
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Create npcap sdk folder
      working-directory: ${{runner.workspace}}\
      run: mkdir pcap

    - name: Download npcap sdk
      working-directory: ${{runner.workspace}}\pcap
      run: curl -o ./npcap-sdk-1.13.zip https://npcap.com/dist/npcap-sdk-1.13.zip

    - name: unzip npcap sdk
      working-directory: ${{runner.workspace}}\pcap
      run: unzip npcap-sdk-1.13.zip

    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y-%m-%dT%H%M')" >> $GITHUB_OUTPUT

    - name: Create build environment
      run: cmake -E make_directory ${{runner.workspace}}\build

    - name: Configure CMake
      shell: bash
      working-directory: ${{runner.workspace}}/build
      run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=Release  -DPCAP_INCLUDE_DIR="${{runner.workspace}}/pcap/Include/"  -DPCAP_LIBRARY="${{runner.workspace}}/pcap/Lib/x64/wpcap.lib"

    - name: Build
      working-directory: ${{runner.workspace}}\build
      shell: bash
      run: cmake --build . --config Release
      env:
        MAKEFLAGS: "-j2"

    - name: Test run
      working-directory: ${{runner.workspace}}\build
      run: ${{runner.workspace}}\build\Release\alphabox.exe

    - name: Upload Alphabox Binary
      uses: actions/upload-artifact@v4
      with:
        name: Alphabox-windows-x86-msvc-${{ env.BUILD_DATE }}.exe
        path: ${{runner.workspace}}\build\Release\alphabox.exe
      env:
        BUILD_DATE: ${{ steps.date.outputs.date }}

  osx-arm64-appleclang:
    runs-on: macos-15
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y-%m-%dT%H%M')" >> $GITHUB_OUTPUT

    - name: Create build environment
      run: cmake -E make_directory ${{runner.workspace}}/build

    - name: Install dependencies
      run: brew install libpcap netcat gnu-sed

    - name: Configure CMake
      shell: bash
      working-directory: ${{runner.workspace}}/build
      # SDL3 comes statically from the third_party/SDL submodule.
      run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_FLAGS="-Wall" -DPCAP_INCLUDE_DIR="$(brew --prefix libpcap)/include" -DPCAP_LIBRARY="$(brew --prefix libpcap)/lib/libpcap.dylib"

    - name: Build
      working-directory: ${{runner.workspace}}/build
      shell: bash
      run: cmake --build . --config Release
      env:
        MAKEFLAGS: "-j3"

    - name: Upload Alphabox Binary
      uses: actions/upload-artifact@v4
      with:
        name: Alphabox-macos-arm64-appleclang-${{ env.BUILD_DATE }}
        path: ${{runner.workspace}}/build/alphabox
      env:
        BUILD_DATE: ${{ steps.date.outputs.date }} 

